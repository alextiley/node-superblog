//- Needs refactoring

- if (messages.errors !== undefined)
	- if (messages.errors.length > 0)
		.alert-box.alert
			- if (messages.errors.length === 1) {
				.message #{messages.errors[0]}
			- } else {
				ul.messages
					each error in messages.errors
						li #{error}
			- }

- if (messages.info !== undefined)
	- if (messages.info.length > 0)
		.alert-box
			- if (messages.info.length === 1) {
				.message #{messages.info[0]}
			- } else {
				ul.messages
					each message in messages.info
						li #{message}
			- }

- if (messages.success !== undefined)
	- if (messages.success.length > 0)
		.alert-box.success
			- if (messages.success.length === 1) {
				.message #{messages.success[0]}
			- } else {
				ul.messages
					each message in messages.success
						li #{message}
			- }